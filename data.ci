mainmodule data
{

	mainchare Main 
	{
		entry Main(CkArgMsg* msg);
		entry void finishProgram();
	}

	group Testers 
	{
		entry Testers(std::string fname);
		entry void readFile(int buffer_tag);
		entry void permuteData(int buffer_tag, double zero_start, size_t my_data_size, nocopypost char ncpyPost[my_data_size]);
	}


}
